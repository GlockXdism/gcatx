local CoreGui = game:GetService("CoreGui"); local TweenService = game:GetService("TweenService"); local Players = game:GetService("Players"); local ReplicatedStorage = game:GetService("ReplicatedStorage"); local HttpService = game:GetService("HttpService");local BASE_SERVER_URL = "https://gcat4.onrender.com"; local my_real_hwid = game:GetService("RbxAnalyticsService"):GetClientId();if setclipboard then setclipboard(tostring(my_real_hwid)) end;pcall(function() if CoreGui:FindFirstChild("GcatHubScreen") then CoreGui.GcatHubScreen:Destroy() end end);local GcatHubScreen = Instance.new("ScreenGui"); GcatHubScreen.Name = "GcatHubScreen"; GcatHubScreen.Parent = CoreGui; GcatHubScreen.ZIndexBehavior = Enum.ZIndexBehavior.Sibling;local AuthFrame = Instance.new("Frame"); AuthFrame.Name = "AuthFrame"; AuthFrame.Size = UDim2.new(0, 340, 0, 180); AuthFrame.Position = UDim2.new(0.5, -170, 0.5, -90); AuthFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18); AuthFrame.BorderSizePixel = 0; AuthFrame.Active = true; AuthFrame.Draggable = true; AuthFrame.Parent = GcatHubScreen;local AuthCorner = Instance.new("UICorner"); AuthCorner.CornerRadius = UDim.new(0, 10); AuthCorner.Parent = AuthFrame;local AuthStroke = Instance.new("UIStroke"); AuthStroke.Color = Color3.fromRGB(0, 140, 255); AuthStroke.Thickness = 1.5; AuthStroke.Parent = AuthFrame;local AuthTitle = Instance.new("TextLabel"); AuthTitle.Size = UDim2.new(1, 0, 0, 40); AuthTitle.BackgroundTransparency = 1; AuthTitle.Text = "🔐 GCAT HUB LICENSE VERIFY"; AuthTitle.TextColor3 = Color3.fromRGB(0, 180, 255); AuthTitle.TextSize = 14; AuthTitle.Font = Enum.Font.GothamBold; AuthTitle.Parent = AuthFrame;local KeyInput = Instance.new("TextBox"); KeyInput.Size = UDim2.new(1, -40, 0, 36); KeyInput.Position = UDim2.new(0, 20, 0, 55); KeyInput.BackgroundColor3 = Color3.fromRGB(25, 25, 30); KeyInput.BorderSizePixel = 0; KeyInput.PlaceholderText = "봇에게 발급받은 KEY 입력"; KeyInput.PlaceholderColor3 = Color3.fromRGB(100, 100, 110); KeyInput.Text = ""; KeyInput.TextColor3 = Color3.fromRGB(255, 255, 255); KeyInput.TextSize = 12; KeyInput.Font = Enum.Font.Gotham; KeyInput.ClearTextOnFocus = false; KeyInput.Parent = AuthFrame;local KeyCorner = Instance.new("UICorner"); KeyCorner.CornerRadius = UDim.new(0, 6); KeyCorner.Parent = KeyInput;local KeyStroke = Instance.new("UIStroke"); KeyStroke.Color = Color3.fromRGB(45, 45, 55) ;KeyStroke.Thickness = 1; KeyStroke.Parent = KeyInput;local VerifyBtn = Instance.new("TextButton"); VerifyBtn.Size = UDim2.new(1, -40, 0, 36); VerifyBtn.Position = UDim2.new(0, 20, 0, 105); VerifyBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 255); VerifyBtn.Text = "인증 및 잠금 해제"; VerifyBtn.TextColor3 = Color3.fromRGB(255, 255, 255); VerifyBtn.TextSize = 13; VerifyBtn.Font = Enum.Font.GothamBold; VerifyBtn.Parent = AuthFrame;local VerifyCorner = Instance.new("UICorner"); VerifyCorner.CornerRadius = UDim.new(0, 6); VerifyCorner.Parent = VerifyBtn;local StatusLabel = Instance.new("TextLabel"); StatusLabel.Size = UDim2.new(1, 0, 0, 20); StatusLabel.Position = UDim2.new(0, 0, 0, 150); StatusLabel.BackgroundTransparency = 1; StatusLabel.Text = "클립보드에 내 HWID가 자동 복사되었습니다."; StatusLabel.TextColor3 = Color3.fromRGB(150, 150, 160); StatusLabel.TextSize = 10; StatusLabel.Font = Enum.Font.Gotham; StatusLabel.Parent = AuthFrame;local MainFrame = Instance.new("Frame"); MainFrame.Name = "MainFrame"; MainFrame.Size = UDim2.new(0, 520, 0, 480); MainFrame.Position = UDim2.new(0.5, -260, 0.5, -240); MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 18); MainFrame.BorderSizePixel = 0; MainFrame.Active = true; MainFrame.Draggable = true; MainFrame.Visible = false; MainFrame.Parent = GcatHubScreen;local MainCorner = Instance.new("UICorner"); MainCorner.CornerRadius = UDim.new(0, 12); MainCorner.Parent = MainFrame;local UIStroke = Instance.new("UIStroke"); UIStroke.Color = Color3.fromRGB(0, 160, 255); UIStroke.Thickness = 1.5; UIStroke.ApplyStrokeMode = Enum.ApplyStrokeMode.Border; UIStroke.Parent = MainFrame;local TitleText = Instance.new("TextLabel"); TitleText.Size = UDim2.new(1, -50, 0, 45); TitleText.Position = UDim2.new(0, 18, 0, 0); TitleText.BackgroundTransparency = 1; TitleText.Text = "🐱 Gcat HUB"; TitleText.TextColor3 = Color3.fromRGB(0, 180, 255); TitleText.TextSize = 18; TitleText.Font = Enum.Font.GothamBold; TitleText.TextXAlignment = Enum.TextXAlignment.Left; TitleText.Parent = MainFrame;local CloseBtn = Instance.new("TextButton"); CloseBtn.Size = UDim2.new(0, 24, 0, 24); CloseBtn.Position = UDim2.new(1, -38, 0, 11); CloseBtn.BackgroundColor3 = Color3.fromRGB(240, 70, 70); CloseBtn.Text = "×"; CloseBtn.TextColor3 = Color3.fromRGB(255, 255, 255); CloseBtn.TextSize = 16; CloseBtn.Font = Enum.Font.GothamBold; CloseBtn.Parent = MainFrame;CloseBtn.MouseButton1Click:Connect(function() GcatHubScreen:Destroy() end);local LeftPanel = Instance.new("Frame"); LeftPanel.Size = UDim2.new(0, 230, 1, -60); LeftPanel.Position = UDim2.new(0, 18, 0, 45); LeftPanel.BackgroundTransparency = 1; LeftPanel.Parent = MainFrame;local function createCustomInput(placeholder, yPos, parent) local box = Instance.new("TextBox"); box.Size = UDim2.new(1, 0, 0, 36); box.Position = UDim2.new(0, 0, 0, yPos); box.BackgroundColor3 = Color3.fromRGB(25, 25, 30); box.BorderSizePixel = 0; box.PlaceholderText = placeholder; box.PlaceholderColor3 = Color3.fromRGB(100, 100, 110); box.Text = ""; box.TextColor3 = Color3.fromRGB(255, 255, 255); box.TextSize = 13; box.Font = Enum.Font.Gotham; box.ClearTextOnFocus = false; box.Parent = parent; local c = Instance.new("UICorner"); c.CornerRadius = UDim.new(0, 6); c.Parent = box; local s = Instance.new("UIStroke"); s.Color = Color3.fromRGB(45, 45, 55); s.Thickness = 1; s.Parent = box; box.Focused:Connect(function() TweenService:Create(s, TweenInfo.new(0.2), {Color = Color3.fromRGB(0, 140, 255)}):Play() end); box.FocusLost:Connect(function() TweenService:Create(s, TweenInfo.new(0.2), {Color = Color3.fromRGB(45, 45, 55)}):Play() end); return box end;local InputUser = createCustomInput("받을 사람 닉네임 입력", 0, LeftPanel); local InputItem = createCustomInput("아이템 이름 입력", 45, LeftPanel); local InputAmount = createCustomInput("보낼 수량 입력", 90, LeftPanel); local InputNote = createCustomInput("우편 메시지 (생략 가능)", 135, LeftPanel);local LogFrame = Instance.new("ScrollingFrame"); LogFrame.Size = UDim2.new(1, 0, 0, 170); LogFrame.Position = UDim2.new(0, 0, 0, 185); LogFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 12); LogFrame.BorderSizePixel = 0; LogFrame.CanvasSize = UDim2.new(0, 0, 0, 0); LogFrame.ScrollBarThickness = 2; LogFrame.ScrollBarImageColor3 = Color3.fromRGB(0, 160, 255); LogFrame.Parent = LeftPanel;local LogCorner = Instance.new("UICorner"); LogCorner.CornerRadius = UDim.new(0, 6); LogCorner.Parent = LogFrame;local LogListLayout = Instance.new("UIListLayout"); LogListLayout.Padding = UDim.new(0, 4); LogListLayout.SortOrder = Enum.SortOrder.LayoutOrder; LogListLayout.Parent = LogFrame;local function appendLog(text, isError) local logText = Instance.new("TextLabel"); logText.Size = UDim2.new(1, -10, 0, 18); logText.BackgroundTransparency = 1; logText.Text = " [" .. os.date("%X") .. "] " .. text; logText.TextColor3 = isError and Color3.fromRGB(255, 90, 90) or Color3.fromRGB(130, 220, 130); logText.TextSize = 11; logText.Font = Enum.Font.Code; logText.TextXAlignment = Enum.TextXAlignment.Left; logText.Parent = LogFrame; LogFrame.CanvasSize = UDim2.new(0, 0, 0, LogListLayout.AbsoluteContentSize.Y + 10); LogFrame.CanvasPosition = Vector2.new(0, LogListLayout.AbsoluteContentSize.Y) end;local RightPanel = Instance.new("Frame"); RightPanel.Size = UDim2.new(0, 236, 1, -60); RightPanel.Position = UDim2.new(1, -254, 0, 45); RightPanel.BackgroundColor3 = Color3.fromRGB(20, 20, 25); RightPanel.BorderSizePixel = 0; RightPanel.Parent = MainFrame;local RightCorner = Instance.new("UICorner"); RightCorner.CornerRadius = UDim.new(0, 8); RightCorner.Parent = RightPanel;local CatalogLabel = Instance.new("TextLabel"); CatalogLabel.Size = UDim2.new(1, 0, 0, 28) ;CatalogLabel.BackgroundTransparency = 1; CatalogLabel.Text = "📦 가방 아이템 터치 선택"; CatalogLabel.TextColor3 = Color3.fromRGB(160, 160, 170); CatalogLabel.TextSize = 12; CatalogLabel.Font = Enum.Font.GothamBold; CatalogLabel.Parent = RightPanel;local CatalogScroll = Instance.new("ScrollingFrame"); CatalogScroll.Size = UDim2.new(1, -12, 1, -34); CatalogScroll.Position = UDim2.new(0, 6, 0, 28); CatalogScroll.BackgroundTransparency = 1; CatalogScroll.BorderSizePixel = 0; CatalogScroll.CanvasSize = UDim2.new(0, 0, 0, 0); CatalogScroll.ScrollBarThickness = 3; CatalogScroll.ScrollBarImageColor3 = Color3.fromRGB(0, 160, 255); CatalogScroll.Parent = RightPanel;local Grid = Instance.new("UIGridLayout"); Grid.CellSize = UDim2.new(0, 106, 0, 40); Grid.CellPadding = UDim2.new(0, 6, 0, 6); Grid.SortOrder = Enum.SortOrder.LayoutOrder; Grid.Parent = CatalogScroll;local LocalPlayer = Players.LocalPlayer; local Networking = require(ReplicatedStorage:WaitForChild("SharedModules"):WaitForChild("Networking")); local PlayerStateClient = require(ReplicatedStorage:WaitForChild("ClientModules"):WaitForChild("PlayerStateClient"));local CATEGORIES = {"Seeds", "Sprinklers", "WateringCans", "Trowels", "Mushrooms", "Raccoons", "Gnomes", "HarvestedFruits", "Pets"};local function getInventory() local replica = PlayerStateClient:GetLocalReplica(); return replica and replica.Data and replica.Data.Inventory end;local function refreshCatalogUi() for _, child in pairs(CatalogScroll:GetChildren()) do if child:IsA("TextButton") then child:Destroy() end end; local inv = getInventory(); if not inv then return end; for _, cat in pairs(CATEGORIES) do local bucket = inv[cat]; if type(bucket) == "table" then for key, entry in pairs(bucket) do local name = tostring(key); local count = 0; if cat == "Pets" and type(entry) == "table" then name = entry.Name or entry.Species or name; count = 1 elseif cat == "HarvestedFruits" and type(entry) == "table" then name = entry.Name or name; count = entry.Count or 1 elseif type(entry) == "number" then count = entry end; if count > 0 then local itemBtn = Instance.new("TextButton"); itemBtn.BackgroundColor3 = Color3.fromRGB(30, 30, 36); itemBtn.Text = name .. "\n(" .. tostring(count) .. "개)"; itemBtn.TextColor3 = Color3.fromRGB(220, 220, 230); itemBtn.TextSize = 11; itemBtn.Font = Enum.Font.Gotham; itemBtn.Parent = CatalogScroll; local bc = Instance.new("UICorner"); bc.CornerRadius = UDim.new(0, 4); bc.Parent = itemBtn; itemBtn.MouseButton1Click:Connect(function() InputItem.Text = name; InputAmount.Text = tostring(count); appendLog("선택완료: " .. name, false) end) end end end end; CatalogScroll.CanvasSize = UDim2.new(0, 0, 0, Grid.AbsoluteContentSize.Y + 10) end;local function buildBatch(inv, name, amt) local out = {}; local max = 20; local want = math.max(1, math.floor(tonumber(amt) or 1)); if type(inv.Pets) == "table" then for key, p in pairs(inv.Pets) do if want <= 0 or #out >= max then break end; if type(p) == "table" and (p.Name == name or p.Species == name) and not p.Equipped then out[#out + 1] = { Category = "Pets", ItemKey = key, Count = 1 }; want = want - 1 end end end; if want > 0 then for _, cat in pairs(CATEGORIES) do if cat ~= "Pets" then local t = inv[cat]; if type(t) == "table" then for k, v in pairs(t) do if tostring(k) == name or (type(v) == "table" and v.Name == name) then local has = type(v) == "number" and v or (type(v) == "table" and v.Count or 1); if has > 0 then out[#out + 1] = { Category = cat, ItemKey = k, Count = math.min(want, has) }; break end end end end end end end; return out end;local function sendMail() local target = InputUser.Text; local itemName = InputItem.Text; local amount = tonumber(InputAmount.Text) or 0; local note = InputNote.Text; if target == "" or itemName == "" or amount <= 0 then appendLog("빈 입력란이 있습니다.", true); return end; local inv = getInventory(); if not inv then appendLog("인벤토리 로드 실패.", true); return end; local batch = buildBatch(inv, itemName, amount); if #batch == 0 then appendLog("가방에 존재하지 않는 아이템입니다.", true); return end; appendLog("대상 조회 중: " .. target, false); local ok, uid = pcall(function() return Networking.Mailbox.LookupPlayer:Fire(target) end); if not ok or type(uid) ~= "number" or uid <= 0 then appendLog("플레이어를 찾을 수 없습니다.", true); return end; appendLog("우편 전송 패킷 발송 중...", false); local success, msg; ok, success, msg = pcall(function() return Networking.Mailbox.SendBatch:Fire(uid, batch, note) end); if success == true then appendLog("우편 발송 완료 -> " .. target, false); task.delay(1, refreshCatalogUi) else appendLog("실패: " .. tostring(msg or success), true) end end;local SendBtn = Instance.new("TextButton"); SendBtn.Size = UDim2.new(1, -36, 0, 42); SendBtn.Position = UDim2.new(0, 18, 0, 415); SendBtn.BackgroundColor3 = Color3.fromRGB(0, 140, 255); SendBtn.Text = "SEND MAIL"; SendBtn.TextColor3 = Color3.fromRGB(255, 255, 255); SendBtn.TextSize = 14; SendBtn.Font = Enum.Font.GothamBold; SendBtn.Parent = MainFrame; local BtnCorner = Instance.new("UICorner"); BtnCorner.CornerRadius = UDim.new(0, 6); BtnCorner.Parent = SendBtn; SendBtn.MouseEnter:Connect(function() TweenService:Create(SendBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 170, 255)}):Play() end); SendBtn.MouseLeave:Connect(function() TweenService:Create(SendBtn, TweenInfo.new(0.2), {BackgroundColor3 = Color3.fromRGB(0, 140, 255)}):Play() end); SendBtn.MouseButton1Click:Connect(function() sendMail() end);local function startGcatAuthentication(user_provided_key) StatusLabel.Text = "보안 서버와 기기 교차 검증 대조 중..."; StatusLabel.TextColor3 = Color3.fromRGB(240, 180, 50); local request_full_url = BASE_SERVER_URL .. "/verify?key=" .. tostring(user_provided_key) .. "&hwid=" .. HttpService:UrlEncode(my_real_hwid); local success, response = pcall(function() return game:HttpGet(request_full_url) end); if success and response then local ok, data = pcall(function() return HttpService:JSONDecode(response) end); if ok and data and data.success == true then StatusLabel.Text = "인증 성공! 잠금을 해제합니다."; StatusLabel.TextColor3 = Color3.fromRGB(100, 240, 100); task.wait(0.5); AuthFrame:Destroy(); MainFrame.Visible = true; task.spawn(function() appendLog("인벤토리 카탈로그 동기화 중...", false); for i = 1, 20 do if getInventory() then break end; task.wait(0.5) end; refreshCatalogUi(); appendLog("카탈로그 스캔 완료.", false) end) else local errMsg = (data and data.message) or "유효하지 않은 KEY 또는 다른 기기에 귀속됨"; LocalPlayer:Kick("❌ [Gcat HUB 보안 위반 차단]: " .. tostring(errMsg)) end else StatusLabel.Text = "서버 통신 실패. Render 구동을 확인하세요."; StatusLabel.TextColor3 = Color3.fromRGB(245, 90, 90) end end;VerifyBtn.MouseButton1Click:Connect(function() local input_text = KeyInput.Text:gsub("%s+", ""); if input_text == "" then StatusLabel.Text = "KEY를 입력해 주세요!"; StatusLabel.TextColor3 = Color3.fromRGB(245, 90, 90); return end; startGcatAuthentication(input_text) end);
